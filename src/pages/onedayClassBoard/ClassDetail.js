import {
  onedayClass,
  updateOnedayClass,
  deleOnedayClass,
} from "../../api/onedayClass";

const ClassDetail = () => {
  const viewClass = async () => {
    const result = await onedayClass();
  };

  return <></>;
};

export default ClassDetail;
